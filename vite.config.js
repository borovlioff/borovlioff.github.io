import { defineConfig } from 'vite';

export default defineConfig({
  // Можно добавить настройки, если нужно, например, алиасы
  // resolve: {
  //   alias: {
  //     '@': '/src',
  //   },
  // },
});
